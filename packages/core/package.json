{
  "name": "@wisp/core",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-free sketching core: document model, history, stroke geometry. No runtime dependencies, so it runs unchanged in a browser, a Web Worker, or the desktop compute host.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
