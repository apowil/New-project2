{
  "name": "@wisp/ops",
  "version": "0.1.0",
  "private": true,
  "description": "Heavy operations that need a geometry library — booleans today. Shared so the tab, the worker and the desktop host run the identical implementation.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@wisp/core": "*",
    "three": "^0.185.0",
    "three-bvh-csg": "^0.0.18",
    "three-mesh-bvh": "^0.9.14"
  }
}
